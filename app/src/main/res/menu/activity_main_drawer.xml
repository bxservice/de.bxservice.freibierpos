<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/open_orders"
            android:icon="@android:drawable/ic_menu_today"
            android:title="@string/title_activity_view_open_orders" />

        <!-- Login progress<item android:id="@+id/nav_reservation" android:icon="@android:drawable/ic_menu_gallery"
            android:title="@string/reservation" /> -->

        <item android:id="@+id/nav_report"
            android:icon="@android:drawable/ic_menu_agenda"
            android:title="@string/report" />

        <!-- <item android:id="@+id/nav_settings" android:icon="@android:drawable/ic_menu_info_details"
            android:title="@string/action_settings" /> -->
    </group>

    <item android:title="@string/comm_category">
        <menu>
            <item android:id="@+id/nav_share" android:icon="@android:drawable/ic_menu_share"
                android:title="@string/share" />
            <item android:id="@+id/nav_send" android:icon="@android:drawable/ic_menu_send"
                android:title="@string/synchronize" />
            <item android:id="@+id/nav_logout" android:icon="@android:drawable/ic_lock_power_off"
                android:title="@string/logout" />
        </menu>
    </item>

</menu>